CREATE TABLE MEMBER(
	USER_ID VARCHAR2(100) NOT NULL,
	USER_PASS VARCHAR2(100) NOT NULL,
	USER_NAME VARCHAR2(300) NOT NULL,
	USER_EMAIL VARCHAR2(300) NOT NULL,
	CONSTRAINT PK_MEMBER_USER_ID PRIMARY KEY(USER_ID)
);

select * from member;

SELECT COUNT(*) FROM MEMBER
	WHERE USER_ID = 'GUESTA' AND USER_PASS = '1234';
	
	SELECT B.*		
FROM (SELECT A.*, ROWNUM AS RNUM 
FROM (SELECT * FROM RECORD ORDER BY RECORD_READINGAVG DESC) A) B
WHERE RNUM BETWEEN 1 AND 10;

INSERT INTO RECORD
VALUES(RECORDSEQ.NEXTVAL,'DONGYOUNG','코로나를위한책','50,51',1,1000,20000,sysdate);
